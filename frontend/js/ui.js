export function toast(msg, kind){\n  const root = document.getElementById('toastRoot');\n  const t = document.createElement('div');\n  t.className = 'toast'+(kind==='ok'?' ok':kind==='warn'?' warn':'');\n  t.textContent = msg;\n  root.appendChild(t);\n  setTimeout(()=>t.remove(), 3400);\n}\n\nexport function closeModal(){ document.getElementById('modalRoot').innerHTML=''; }\nexport function openModal(html, wide){\n  document.getElementById('modalRoot').innerHTML =\n    '<div class="modalOverlay" onmousedown="if(event.target===this) closeModal()">'+\n      '<div class="modal'+(wide?' wide':'')+'">'+html+'</div>'+\n    '</div>';\n}\n\nexport const body = ()=> document.getElementById('appBody');\n